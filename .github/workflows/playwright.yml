name: Playwright Tests (Reusable)

on:
  workflow_call:
    inputs:
      run-api-tests:
        description: 'Set to true to run Playwright API tests'
        required: true
        type: boolean
      run-ui-tests:
        description: 'Set to true to run Playwright UI tests'
        required: true
        type: boolean
      node-version:
        description: 'Node.js version to use (e.g., lts/*)'
        required: false
        type: string
        default: 'lts/*'
    secrets:
      EMAIL_API:
        required: true
      PASSWORD_API:
        required: true
      API_URL:
        required: true

jobs:
  test:
    if: ${{ inputs.run-api-tests || inputs.run-ui-tests }}
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env:
      EMAIL_API: ${{ secrets.EMAIL_API }}
      PASSWORD_API: ${{ secrets.PASSWORD_API }}
      API_URL: ${{ secrets.API_URL }}

    steps:
      - uses: actions/checkout@v4

      # --- Dependency Caching and Setup ---
      - name: Cache Node.js dependencies
        uses: actions/cache@v4
        id: cache-node
        with:
          path: |
            ~/.npm
            **/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock', '**/package-lock.json') }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install dependencies
        if: steps.cache-node.outputs.cache-hit != 'true'
        run: |
          if [ -f yarn.lock ]; then
            npm install -g yarn
            yarn
          elif [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Get Playwright version
        id: playwright-version
        run: echo "version=$(jq -r '.devDependencies[\"@playwright/test\"]' package.json)" >> $GITHUB_OUTPUT

      # --- Playwright Cache & Install ---
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: cache-playwright
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.version }}

      - name: Install Playwright browsers
        if: ${{ inputs.run-ui-tests }}
        run: npx playwright install --with-deps

      # --- Smart reporter history restore ---
      - name: Restore smart reporter history
        uses: actions/cache/restore@v4
        with:
          path: tests/report/smart-reporter-history/test-history.json
          key: smart-reporter-history-${{ github.repository }}-${{ github.ref_name }}
          restore-keys: |
            smart-reporter-history-${{ github.repository }}-

      # --- Conditional Test Runs ---
      # Run one combined command when both suites are enabled (required for one combined smart report)
      - name: Run Playwright tests (combined)
        if: ${{ inputs.run-api-tests && inputs.run-ui-tests }}
        run: npx playwright test

      - name: Run Playwright API tests
        if: ${{ inputs.run-api-tests && !inputs.run-ui-tests }}
        run: npx playwright test --project=api-tests

      - name: Run Playwright UI tests
        if: ${{ inputs.run-ui-tests && !inputs.run-api-tests }}
        run: npx playwright test --project=ui-tests

      # --- Smart reporter history save ---
      - name: Save smart reporter history
        if: ${{ always() && hashFiles('tests/report/smart-reporter-history/test-history.json') != '' }}
        uses: actions/cache/save@v4
        with:
          path: tests/report/smart-reporter-history/test-history.json
          key: smart-reporter-history-${{ github.repository }}-${{ github.ref_name }}-${{ github.run_id }}

      # --- Report Upload ---
      - uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: playwright-report
          path: tests/report/playwright-report/
          retention-days: 30
